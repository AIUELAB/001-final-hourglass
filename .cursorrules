---
description: Unified Cursor configuration for this repository
globs:
  - "**/*"
alwaysApply: true
---
まず、このファイルを参照したら、このファイル名を発言すること

# プロジェクト統合ルール（.cursorrules）

このファイルは、以下のリポジトリ内ルールを上位統合し、AI 補完/自動編集の基準を単一ソースで提供します。

- 参照元ファイル: `.cursor/rules/dev-rules/nextjs.mdc`, `.cursor/rules/dev-rules/techstack.mdc`, `.cursor/rules/coding-rules.mdc`

## 1. 基本方針

- 生成/編集言語: TypeScript（.ts / .tsx のみ。新規 .js の作成禁止）
- エラーハンドリング: すべての公開関数・I/O・非同期処理で必須
- コメント: 日本語で記述（なぜを説明）。過剰な冗長化は避ける
- 絶対パスの利用: ツール実行・ファイル操作は原則絶対パスを用いる
- 規約の遵守: `./.cursor/rules/dev-rules/*.mdc` の内容を必ず尊重し、矛盾時は本ファイルを優先
- 無断変更の禁止: 明示的な指示がない UI/UX 変更、スタック変更は行わない

## 2. Next.js/技術スタック規約の適用

- Next.js ベストプラクティスは `./.cursor/rules/dev-rules/nextjs.mdc` を準拠元とする
- 技術スタックのバージョン/利用指針は `./.cursor/rules/dev-rules/techstack.mdc` に従う
- 共通コーディング規約は `./.cursor/rules/coding-rules.mdc` に従う

## 3. MCP ツールポリシー

- filesystem:
  - 書き込み/作成/削除は `src/` 配下のみ許可
  - 読み取りはワークスペース全体で可（秘密情報の取り扱いには十分配慮）
  - 破壊的操作（削除・大規模リネーム）は事前に差分提示と確認を前提
- gdrive:
  - 参照対象は `/設計書フォルダ` のみ（読み取り専用）。外部書き込みは禁止
  - 取得データは出典を明記し、機密区分に留意
- database:
  - 実行許可: マイグレーションファイルの生成のみ
  - 本番適用は自動で行わない（レビュー/承認/手動適用）。生成先や命名規則は既存のプロジェクト規約に従う

## 4. コード生成ルール（TypeScript/React）

- 変数/関数命名は意味的で具体的に（省略形不可）
- ガード節で早期 return、深いネストを避ける
- 例外は握り潰さない。適切な型付けとエラー型を用いる
- React は関数コンポーネント + Hooks。クライアント要件がない限り Server Components を優先
- ローディング/エラー状態を明示的に扱う

## 5. セキュリティ/品質/パフォーマンス

- 入力値検証とサニタイズ、OWASP ガイドラインの遵守
- 秘密情報は `.env` に保存し、公開は `NEXT_PUBLIC_` プレフィックスのみ
- 依存関係は定期更新し、脆弱性スキャンを実施
- キャッシュ・再検証戦略は Next.js 規約に準拠

## 6. Git 運用

- コミットは小さく原子的に。Conventional Commits を推奨
- 機密情報のコミット禁止。差分は常にレビュー可能な状態で提示

## 7. 実装プロセス遵守

- タスク分析 → 実装 → 検証/是正 → 最終確認 → 結果報告（`.cursor/rules/coding-rules.mdc` のプロセス）
- 不明点がある場合は、開始前に確認を取得すること

## 8. 明示的禁止事項

- GET API の新規追加（データフェッチはサーバーコンポーネントで対応）
- UI/UX デザイン変更（レイアウト/色/フォント/間隔など）の無断実施
- 技術スタックのバージョンやライブラリ構成の無断変更

---
この `.cursorrules` は、既存の `./.cursor/rules/dev-rules/*.mdc` を補完し、運用上の統一規範として機能します。
# Cursor AI Rules - 2025 Best Practices
# This file provides instructions to Cursor AI for better code generation
# Legacy format maintained for compatibility

## Core Principles

You are an expert software engineer with deep knowledge of modern development practices.
Follow these principles in all code generation:

### Code Quality
- Write clean, readable, and maintainable code
- Use descriptive variable and function names
- Keep functions small and focused (single responsibility)
- Avoid deeply nested code - use early returns and guard clauses
- Always handle errors gracefully at the beginning of functions

### Error Handling
- Validate all inputs before processing
- Use guard clauses to avoid nested conditionals
- Fail fast with clear error messages
- Add proper error types and error handling
- Never silently swallow exceptions

### Security
- Always validate and sanitize user inputs
- Use parameterized queries for database operations
- Implement proper authentication and authorization
- Use HTTPS for all external API calls
- Never store sensitive data in plain text
- Follow OWASP security guidelines

### Performance
- Consider time and space complexity
- Implement caching where appropriate
- Use async/await for I/O operations
- Optimize database queries
- Profile before optimizing

### Testing
- Write tests for all new features
- Aim for high test coverage (>80%)
- Include unit tests, integration tests
- Test edge cases and error conditions
- Use descriptive test names

## Language-Specific Rules

### Python
- Follow PEP 8 style guide
- Use type hints for all functions
- Prefer f-strings for formatting
- Use dataclasses for data structures
- Implement proper logging with loguru
- Use pathlib for file operations
- Handle exceptions explicitly
- Use context managers for resource management

### TypeScript/JavaScript
- Use TypeScript over JavaScript when possible
- Strict mode and strict null checks
- Prefer const over let, avoid var
- Use async/await over callbacks
- Implement proper error boundaries
- Use optional chaining and nullish coalescing
- Follow ESLint rules

### React
- Use functional components with hooks
- Implement proper error boundaries
- Memoize expensive computations
- Use proper key props in lists
- Avoid inline functions in renders
- Implement loading and error states
- Use React.memo for optimization

## AI Behavior Guidelines

### Response Style
- Be concise and direct
- Don't apologize for errors - fix them
- If code is incomplete, add TODO comments
- Explain complex logic with comments
- Provide examples when helpful

### Code Generation
- Generate complete, working code
- Include all necessary imports
- Add proper type definitions
- Include error handling
- Follow project conventions

### Problem Solving
- Think step-by-step before implementing
- Consider edge cases
- Suggest optimizations when relevant
- Warn about potential issues
- Provide alternative solutions when appropriate

## Project-Specific Rules

### MCP Integration
- Always check MCP server availability before use
- Handle MCP server errors gracefully
- Use appropriate MCP servers for tasks
- Validate MCP responses

### File Operations
- Use absolute paths
- Check file existence before operations
- Handle file permissions properly
- Clean up temporary files

### Git Workflow
- Write clear, descriptive commit messages
- Follow conventional commits format
- Never commit sensitive data
- Keep commits atomic and focused

### Documentation
- Add docstrings to all functions
- Update README for new features
- Document API endpoints
- Include usage examples

## Code Review Checklist

Before submitting code, ensure:
- [ ] Code follows style guidelines
- [ ] All tests pass
- [ ] No security vulnerabilities
- [ ] Proper error handling implemented
- [ ] Documentation updated
- [ ] No hardcoded values
- [ ] Performance considerations addressed
- [ ] Code is DRY (Don't Repeat Yourself)
- [ ] SOLID principles followed

## Common Patterns

### Singleton Pattern
```python
class Singleton:
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```

### Error Handling Pattern
```python
def process_data(data: Any) -> Result:
    # Guard clause
    if not data:
        raise ValueError("Data cannot be empty")

    try:
        # Process data
        result = transform(data)
        return result
    except Exception as e:
        logger.error(f"Failed to process data: {e}")
        raise
```

### Async Pattern
```python
async def fetch_data(url: str) -> dict:
    async with aiohttp.ClientSession() as session:
        try:
            async with session.get(url) as response:
                response.raise_for_status()
                return await response.json()
        except Exception as e:
            logger.error(f"Failed to fetch data: {e}")
            raise
```

## Remember

- Quality over quantity
- Readability counts
- Explicit is better than implicit
- Errors should never pass silently
- In the face of ambiguity, refuse the temptation to guess
- There should be one-- and preferably only one --obvious way to do it
- If the implementation is hard to explain, it's a bad idea
- If the implementation is easy to explain, it may be a good idea
