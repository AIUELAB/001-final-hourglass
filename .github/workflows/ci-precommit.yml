name: pre-commit

permissions:
  contents: read

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false  # LFS budget exceeded - disabled temporarily
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Install Node (for prettier etc.)
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install pre-commit
        run: |
          pip install pre-commit
      - name: Run pre-commit
        env:
          # CI環境ではCSVファイルに依存するフックをスキップ（LFSバジェット超過のため）
          SKIP: periphery,check-group-names,check-episode-consistency,check-same-age-duplicates,check-person-duplicates,check-meta-violations,check-fictional-name-format
        run: |
          pre-commit run -a
