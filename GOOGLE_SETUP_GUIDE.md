# 🔐 Google Cloud認証設定ガイド（5分で完了）

## 📋 事前準備
- Googleアカウントが必要です
- ブラウザで以下の作業を行います

## 🚀 手順

### ステップ1: Google Cloud Consoleにアクセス
1. **[Google Cloud Console](https://console.cloud.google.com)** を開く
2. Googleアカウントでログイン

### ステップ2: プロジェクト作成
1. 画面上部の「プロジェクトを選択」をクリック
2. 「新しいプロジェクト」をクリック
3. プロジェクト名: `ultra-think-sheets` （任意）
4. 「作成」をクリック

### ステップ3: APIを有効化
1. 左メニューから「APIとサービス」→「ライブラリ」
2. 検索窓で「Google Sheets API」を検索
3. 「Google Sheets API」をクリック→「有効にする」
4. 同様に「Google Drive API」も検索して有効化

### ステップ4: サービスアカウント作成
1. 左メニュー「APIとサービス」→「認証情報」
2. 「+ 認証情報を作成」→「サービスアカウント」
3. サービスアカウント名: `sheets-sync` （任意）
4. 「作成して続行」→「続行」→「完了」

### ステップ5: JSONキーのダウンロード
1. 作成したサービスアカウント名をクリック
2. 「キー」タブをクリック
3. 「鍵を追加」→「新しい鍵を作成」
4. 「JSON」を選択→「作成」
5. **ダウンロードされたファイルを `credentials.json` にリネーム**
6. **このフォルダ（001-final-hourglass）に移動**

## ✅ 確認
以下のファイルがこのフォルダにあることを確認：
```
001-final-hourglass/
└── credentials.json  ← これがあればOK！
```

## 🎯 次のステップ
```bash
# 再度セットアップを実行
python google_sheets_sync.py --setup
```

## ⚠️ 注意事項
- `credentials.json`は秘密情報です。他人と共有しないでください
- `.gitignore`に追加してGitにコミットしないようにしてください

## 🆘 よくある問題

### 「APIが有効になっていません」エラー
→ ステップ3を再確認。両方のAPI（Sheets & Drive）を有効化

### 「権限がありません」エラー
→ サービスアカウントに適切な権限があるか確認

### 認証ファイルが見つからない
→ `credentials.json`という名前で保存されているか確認

---
準備ができたら、`credentials.json`をこのフォルダに配置して再実行してください！