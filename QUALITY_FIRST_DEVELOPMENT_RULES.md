# 🔴 Ultra Think 開発必須ルール - 品質第一原則

## 📅 制定日: 2025年8月27日

---

## 🎯 最重要原則：品質 > 数量

### ❌ 絶対にやってはいけないこと
1. 品質を犠牲にして数を増やす
2. チェックなしで次の工程に進む
3. ユーザーの条件を無視または忘れる
4. 同じ質問を繰り返しユーザーに聞く

### ✅ 必ず守ること
1. **品質第一** - 品質が担保できない場合は即座に停止
2. **条件確認** - ユーザーの全条件を満たしているか常に確認
3. **リスニング** - ユーザーの要求を正確に理解し記録
4. **記憶保持** - 一度受けた指示は二度と聞き返さない

---

## 📋 開発プロセス必須チェックリスト

### 1. 開始前チェック
```
□ ユーザーの全要求事項をリストアップ
□ 既存のルール文書（PERSON_NAME_DISPLAY_UNIFIED_RULES.md等）を確認
□ 品質基準を明確化
□ チェックポイントを設定
```

### 2. 実装中チェック（100件ごと）
```
□ データ品質は基準を満たしているか？
□ ルール違反はないか？
□ 非人物エントリーが混入していないか？
□ 必須フィールドは正しく設定されているか？
```

### 3. 品質問題発生時の対応
```
IF 品質基準を満たさない THEN
    1. 即座に処理を停止
    2. 問題を分析
    3. 修正方法を検討
    4. 品質を担保できる方法を実装
    5. 小規模テストで確認
    6. 問題なければ再開
```

---

## 🚨 絶対的停止条件

以下の場合は**必ず処理を停止**：

1. **ルール違反検出時**
   - person_name_display違反
   - カテゴリ不正
   - 必須フィールド欠損

2. **品質劣化検出時**
   - プレースホルダー名の生成
   - ランダム値の使用
   - 検証なしのデータ追加

3. **ユーザー条件違反時**
   - 指定された条件を満たさない
   - 要求と異なる処理を実行

---

## 📝 ユーザー要求の永続的記録

### 既に受けた重要指示（二度と聞き返さない）

1. **12,410人は最低ライン**
   - 目標ではなくスタート地点
   - 継続的に拡張すること

2. **name_recognition精度向上戦略**
   - 教育認知度 35%
   - メディア露出 30%
   - SNS言及 20%
   - グローバル認知度 15%
   - JapaneseRecognitionCalibratorを必ず使用

3. **person_name_displayルール**
   - PERSON_NAME_DISPLAY_UNIFIED_RULES.md準拠
   - 歴史上の人物の日本語表記必須
   - 芸名の例外処理（あばれる君、さかなクン等）

4. **品質管理要求**
   - チェック工程をコンスタントに挟む
   - 品質を担保できない場合は停止
   - プレースホルダーではなく実在人物

5. **データ構造要求**
   - 24フィールドのエピソード形式
   - UTF-8 with BOM for Excel
   - 重複は内容充実している方を残す

---

## 🔄 品質保証プロセス

### 段階的実装フロー
```python
def quality_assured_process(batch_size=100):
    while True:
        # 1. 小規模バッチ生成
        batch = generate_batch(batch_size)

        # 2. 品質チェック
        if not quality_check(batch):
            print("❌ 品質基準未達 - 処理停止")
            analyze_and_fix_quality_issues()
            continue

        # 3. ルールチェック
        if not rule_compliance_check(batch):
            print("❌ ルール違反検出 - 処理停止")
            fix_rule_violations()
            continue

        # 4. ユーザー条件チェック
        if not user_requirements_check(batch):
            print("❌ ユーザー要求未達 - 処理停止")
            review_user_requirements()
            continue

        # 5. すべてクリアなら追加
        add_to_database(batch)
        report_progress()
```

---

## 🎧 リスニング状態の維持

### アクティブリスニングの実践

1. **理解の確認**
   - ユーザーの意図を正確に把握
   - 推測や憶測を避ける
   - 不明な点は即座に質問

2. **記憶の保持**
   - 一度聞いたことは記録
   - 同じ質問を繰り返さない
   - 過去の指示を参照

3. **フィードバックループ**
   - 実装前に計画を提示
   - ユーザーの確認を得る
   - 修正要求に即座に対応

---

## ⚠️ 警告事項

### これらの行動は即座に修正が必要

- 数値目標のために品質を妥協する
- 「早く」「急いで」という理由で検証を省略
- ユーザーが何度も同じ指摘をしている
- プレースホルダーやダミーデータを本番に混入
- ランダム値で重要フィールドを埋める

---

## 📌 常に意識すること

> **品質は数量に勝る**
> **一度の指示は永続的**
> **チェックなしに進まない**
> **問題があれば即停止**

---

## 🔴 最終確認

このルールは以下の優先順位で適用：

1. **品質維持** - 最優先
2. **ユーザー要求遵守** - 次点
3. **継続的改善** - 常時
4. **数値達成** - 最低優先度

**数を増やすために品質を落とすことは絶対に許されない。**

---

*このルールは永続的に有効であり、全ての開発において最優先で適用される。*
*Ultra Think Quality First Development Rules v1.0*
