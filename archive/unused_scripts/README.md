# 未使用スクリプトアーカイブ

このディレクトリには、使用されていないスクリプトが保管されています。

## auto_scale_to_8000.py

**移動日：** 2025-12-15
**理由：** 実行ログなし、importなし、git履歴なしで未使用が確認された

**概要：**
8,000人目標達成自動スケーリングスクリプト。
目標人物数に到達するまで継続的にエピソードを生成・統合する。

**未使用の根拠：**
- `logs/` ディレクトリにauto_scale関連のログファイルなし
- `grep -r "import.*auto_scale" scripts/` で0件
- `git log --grep="auto_scale"` で0件

**将来の再利用：**
必要に応じてarchive/から復元可能。
ただし、現在のエピソード生成フローとの整合性を確認すること。
